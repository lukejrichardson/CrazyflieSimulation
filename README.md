# Main.slx is the working file

## Open the .prj file from maltab to open the project and have all parameters loaded into the workspace.

## The crazyflie controller uses NWU axes with inverted pitch. The dynamics also uses NWU but no inverted pitch.
